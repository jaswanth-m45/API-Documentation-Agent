# API-Documentation-Agent
An API Documentation Agent automates sync between backend code and API docs. It monitors Git for route or schema changes, parses ASTs to extract endpoints and models, updates OpenAPI/Swagger specs using LLMs/static analysis, checks for breaking changes, and deploys updated docs (like Swagger UI) via CI/CD pipelines to prevent doc drift.
